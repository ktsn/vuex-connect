language: node_js
cache:
  directories:
    - $HOME/.npm
    - $HOME/.yarn-cache
    - node_modules
node_js:
  - "6"
before_install:
  - npm i -g yarn --cache-min 600000
install:
  - yarn
script:
  - echo
